type Query {
  version: Version
  variant(by_id: IdInput): Variant
  populations(genome_id: String!): [Population]
  statistics(by_id: IdInput): VariantStatistics
}

input IdInput {
  genome_id: String!
  variant_id: String!
}
